// md.renderer.rules.paragraph_open = () => `<p class="mb-3">`;

// md.renderer.rules.heading_open = function(tokens, i) {
//   let str;
//   let token = tokens[i];
  
//   if (token.tag === 'h1') {
//     str = `<h1 class='mb-1'>`;
//   } else {
//     str = `<${token.tag}>`;
//   }

//   return str;
// };
